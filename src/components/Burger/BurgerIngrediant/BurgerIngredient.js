import React from 'react'

const BurgerIngredient = (props) => {

    let ingredient = null; 

    switch(props.type) {
        case('bread-bottom'): 
            ingredient
    }
    return (
        <div>
            
        </div>
    )
}

export default BurgerIngredient
